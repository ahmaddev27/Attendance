'use client';

// Employee-side notifications route — reuses the shared inbox component so
// employees stay inside the (employee) layout instead of being routed into
// the admin shell.
export { NotificationsPage as default } from '@/components/notifications/notifications-page';
