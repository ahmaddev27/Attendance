# Copy to `.env` and adjust for your environment.
# Any EXPO_PUBLIC_* var is inlined into the JS bundle at build time.

# Backend base URL (must end WITHOUT a trailing slash).
EXPO_PUBLIC_API_URL=https://attendees.taqatgaza.com/api
